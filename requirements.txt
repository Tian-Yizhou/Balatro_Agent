# NOTE: Prefer conda environments for setup:
#   conda env create -f environment.yml       (full project)
#   conda env create -f environment_gym.yml   (environment only)
#
# This file is kept for pip-only workflows (pip install -r requirements.txt).

# balatro_gym (environment) — core dependencies
gymnasium>=0.29.0
numpy>=1.24.0
pyyaml>=6.0

# agent (training/baselines) — only needed for agent/ package
torch>=2.0.0
ray[rllib]>=2.10.0

# recording wrappers — only needed for balatro_gym.wrappers
pyarrow>=12.0

# development
pytest>=7.0.0
